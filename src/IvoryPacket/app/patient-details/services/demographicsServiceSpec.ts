"use strict"

beforeEach(angular.mock.module("app"));
beforeEach(angular.mock.module("app.patient"));

var demographicService: interfaces.services.demographicsService;

beforeEach(inject(function () { }));

